version: '3.8'
services:
  nextjs:
    build: .
    ports:
      - "8087:3000"  # Map port 8087 on the host to 3000 in the container
    environment:
      - NODE_ENV=production
    restart: always  # Automatically restart the container if it crashes
